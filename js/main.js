$(document).ready(function () {
    var table = $('#KivaSort').makeKivaTable({
        scrollY: 300,
        autoWidth: true,
        scrollX: true
    }).DataTable();
    });

    $('#tabs').tabs();
});
